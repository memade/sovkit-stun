# Local-only by default. Public exposure must be an explicit deployment decision.
bind_address=127.0.0.1
port_a=3478
port_b=3479
# 0 = until SIGINT/SIGTERM, otherwise 1..86400 seconds.
duration_seconds=0
# Private, source-bound cross-port diagnostic extension; not RFC 5780.
filtering_diagnostics=false
